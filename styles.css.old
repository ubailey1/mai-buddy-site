
/* Base styles */
body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  margin: 0;
  padding: 0;
  background: #fff;
  color: #333;
  line-height: 1.6;
}

/* Hero section */
.hero {
  background: linear-gradient(to bottom, #f5c24b, #fff);
  padding: 4rem 1rem 2rem;
  text-align: center;
}

.hero h1 {
  font-size: 3rem;
  margin: 0;
}

.hero .highlight {
  color: #f5c24b;
  font-weight: bold;
}

.hero p {
  font-size: 1.1rem;
  margin: 1rem auto;
  max-width: 500px;
}

.hero .btn {
  background: #000;
  color: #fff;
  padding: 0.75rem 1.5rem;
  margin-top: 1rem;
  border-radius: 30px;
  text-decoration: none;
  font-weight: 600;
  display: inline-block;
  transition: background 0.3s ease;
}

.hero .btn:hover {
  background: #222;
}

/* Features section */
.features {
  background: #f4f4f4;
  padding: 3rem 1rem;
  display: flex;
  flex-direction: column;
  gap: 2rem;
}

.feature {
  max-width: 700px;
  margin: 0 auto;
  text-align: center;
}

.feature h2 {
  font-size: 1.5rem;
  margin-bottom: 0.5rem;
}

.feature img {
  width: 100px;
  margin-bottom: 1rem;
}

/* About section */
.about {
  padding: 3rem 1rem;
  max-width: 800px;
  margin: 0 auto;
  text-align: center;
}

.about h2 {
  font-size: 2rem;
  margin-bottom: 1rem;
}

/* Footer */
footer {
  background: #eee;
  text-align: center;
  padding: 2rem 1rem;
  font-style: italic;
}
